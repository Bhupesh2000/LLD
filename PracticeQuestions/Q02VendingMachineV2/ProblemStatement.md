V2 scope (what’s new)
Many products, each with its own code (like A1, B2), name, price
Track stock per product
Same simple coins (1,5,10)
Same money flow: insert → select → (optionally) return change
(No “optimal coin change” yet — we’ll add that later.)