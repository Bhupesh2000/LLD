package OOPs.ClassesAndObjects;

// An enum (short for enumeration) is a special data type that defines a fixed set of constants. 
// It’s used when a variable can only take one out of a small set of predefined values.
public enum TreeType {
    OAK,
    MAPLE,
    PECAN,
    WALNUT,
    PINE
}
